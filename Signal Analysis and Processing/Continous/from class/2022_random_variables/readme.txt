The main file is pdf_20xx_v1.m

Click on it to open it.

The code is divided into "sections" delimited by the double comment mark %% at the start and the "return" statement at the end.

To run a section, you should position the cursor inside the section and then hit Crtl-return. Only that section will be executed.

You can edit the internal parameters of each section in Matlab and appreciate the impact of each change.